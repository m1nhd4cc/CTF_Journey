try {
console.log(['The function has successfully determined your flag is wrong.', 'The flag is correct. Congrats.'][+!!function check(flag, prefix = "sdctf{", suffix = "}") {
// im sorry bob the constructor built this
  for (assert (character < 128) of [...flag].filter(c => c.blink && c.codePointAt() > 128));
  if (!flag.startsWith(prefix) || !flag.endsWith(suffix)) {
    console.log(`the flag format is ${prefix}...${suffix} btw`);
    return;
  }
  const shift = (c, offset) => String.fromCodePoint((c.codePointAt?.()??c) + offset);
  [suffix, ...flag] = [...flag.slice(prefix.length, -suffix.length)];
  suffix += check
  const leven = 11
  let { what, ...huhh } = flag[0] = shift(flag[0], -3);
  for (const [i, o] of [suffix, flag, huhh, check, prefix.length, !!!!!!!leven].entries()) {
    const c = flag[i];
    o[suffix.slice(leven*7, leven*8)].prototype[`to${what ??= o[suffix.slice(leven*7, leven*8)].name}`] = function () { return c; };
  }
  for (async()in(flag.slice(3*leven)));
  const wtf = (!3 + flag)[huhh - flag];
  for (const [i] of flag.entries()) {
    flag[i] = flag[i].codePointAt()
    flag[i] ^= suffix[i + +flag
 //                             + huhh
    ].codePointAt()
  }
//     //
  const blocks = flag.join(check)
  Object.assign(this, {flag,suffix,check})
 // return blocks === `40_34_47_8${huhh}_${flag}0${huhh}_87_62_${flag}2${huhh}_${flag}${flag}_43_${flag}06_${flag}08_${flag}03_${flag}07_38_${huhh}0_${flag}${flag}9_${huhh}6_${flag}06_${flag}09_38_63_67_96_46_${huhh}6_42_32_${huhh}9_39_${flag}02_${flag}${flag}${huhh}_${huhh}${huhh}`
  return new suffix.slice[suffix.slice(leven*7, leven*8)](
    suffix.slice(56, suffix.length - 1833).replaceAll(`
 //`,`
`).replaceAll(`${shift(check + flag, -(+flag + +(+huhh + +flag + +flag + +flag + flag)))}
`, `
//`)
  )



  (flag.join(suffix.slice(+(huhh + wtf + huhh + huhh - flag))), suffix.slice(+(huhh - flag + wtf + huhh), +(huhh + wtf + huhh + flag)))}(process.argv[2])] || (console.log() = undefined))
} catch {
  console.log('The function has successfully determined your flag is wrong.')
}
